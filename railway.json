{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan config:cache && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan storage:link || true && php -S 0.0.0.0:$PORT -t public server.php"
  }
}